  spring:
    profiles:
      active: native
    application:
      name: config-server
    cloud:
      config:
        server:
          native:
            search-locations: classpath:/configurations
          git:
            uri: https://github.com/Bob160/e-commerce-app.git

server:
  port: 8888
